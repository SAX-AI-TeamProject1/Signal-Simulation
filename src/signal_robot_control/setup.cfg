# Boilerplate every ament_python package needs: it puts the generated scripts
# where `ros2 run` looks for them (lib/<package>) instead of the default bin/.

[develop]
script_dir=$base/lib/signal_robot_control
[install]
install_scripts=$base/lib/signal_robot_control
